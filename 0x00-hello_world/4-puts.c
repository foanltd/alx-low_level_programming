#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Mycar[60] = "\"Programming is like building a multilingual puzzle";
	puts(Mycar);
	return (0);
}
